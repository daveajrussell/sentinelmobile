<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.sentinel"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COURSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.SET_DEBUG_APP"/>

    <uses-sdk android:minSdkVersion="15"/>
    <uses-feature android:name="android.hardware.camera"/>

    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher">

        <uses-library android:name="com.google.android.maps"/>

        <activity android:name=".app.Sentinel"
                  android:label="@string/app_name"
                  android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".authentication.SentinelLogin"
                  android:theme="@android:style/Theme.NoTitleBar"/>
        <activity android:name=".asset.GeotagDeliveryZXingActvity"
                  android:theme="@android:style/Theme.NoTitleBar"/>
        <activity android:name=".app.SentinelNearingLegalDrivingTimeActivity"
                  android:theme="@android:style/Theme.NoTitleBar"/>

        <service android:name=".tracking.SentinelLocationService"
                 android:enabled="true"
                 android:permission="com.sentinel.LOCATION_SERVICE_PERMISSION"/>
        <service android:name=".app.BreakService"/>

        <receiver android:name=".connection.ConnectionChangedBroadcastReceiver">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
            </intent-filter>
        </receiver>

    </application>
</manifest>
